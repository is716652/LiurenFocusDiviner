# 实现方式决定：资源级 Token（2026-09-16）

> 依据：`鸿蒙规范文档/视觉风格/1-色彩设计规范/04-代码示例/背景代码示范.md` 与
> `2-字体设计规范/04-代码示例/文本样式代码示范.md`、`6-圆角参数/04-代码示例`、`7-间隔参数/04-代码示例`。
> 本文件**修正**《设计令牌清单.md》第七节的"待定"，并纠正其中一处单位误用。

## 一、规范给的做法（原文要点）

| 项 | 规范做法 |
|:--|:--|
| 颜色 Token | 放**资源**里：`resources/base/element/color.json`（浅色）+ `resources/dark/element/color.json`（深色），**Token 名称完全相同、只有色值不同** |
| 组件引用 | 一律 `$r('app.color.xxx')`，**不得硬编码色值** |
| 字号 / 行高 / 圆角 / 间距 | 放 `resources/base/element/float.json`，用 `$r('app.float.xxx')` 引用 |
| 字号单位 | **`fp`（font pixel）**，不是 `vp` —— 只有 `fp` 才随系统字体缩放（无障碍大字体要求） |
| 可选封装 | 再包一层 ArkTS 常量类（`TextStyles` / `SpacingTokens` / `RadiusTokens`）便于复用，但**底层仍是资源 Token** |

## 二、因此本项目的实现方式（定案）

1. **结构色/文字色/品牌金/状态色 → `color.json`（base + dark）**，名称即《设计令牌清单》第二节的 14 个语义名
   （`bg_page`/`bg_card`/`text_strong`/`brand_gold_strong`…）；天地盘那 10 个（`pan_*`）同样进 `color.json`。
2. **排印/圆角/间距 → `float.json`**，字号**改用 `fp`**（修正：令牌清单第四节列的是 vp，实际用 fp）。
3. 组件里一律 `$r('app.color.*')` / `$r('app.float.*')`；**唯一允许出现字面色值的地方是 `color.json`** ——
   这条会做成门禁（"未登记字面色值"判据），杜绝再回到 615 处散落色值的老路。
4. **免费版无需单独维护**：`resources/` 整树由 `sync_free_edition.py` 拷贝同步 ✓（但改完必须跑
   `_ets_pipeline.js` 再编免费版 —— 这是踩过的坑：不跑就编出"旧代码却 BUILD SUCCESSFUL" ✗）。

## 三、切换方式（已拍板 2026-09-16：**乙 · 应用内开关，默认深色**）

用 `base` + `dark` 资源 Token 时，浅色/深色本来由系统自动切换（甲）。但用户诉求是
「**默认保持现在的深色**、可选浅色」，故选定乙。两者不是二选一地对立的：

| 方案 | 说明 | 代价 |
|:--|:--|:--|
| 甲：跟随系统 | base=浅色、dark=深色，系统切我也切 | 最省事；但浅色系统里打开就是浅色，与"默认深色"不符 |
| **乙：应用内开关（默认深色）** ✅**选定** | 应用内选深色（默认）/ 浅色；仍用 base=浅色、dark=深色 两套资源承载 | 需自管主题状态并覆盖资源解析 |

**乙 的落地机制（已查 SDK 确认，不是猜的）**：

```
ApplicationContext.setColorMode(colorMode: ConfigurationConstant.ColorMode): void
  · @since 11（本项目 SDK 满足）· 只允许主线程调用
  · SDK 原文：Target color mode, including dark mode, light mode, and system theme mode (no setting)
  声明处：D:\HarmonyOS\command-line-tools-6.1.1-release\sdk\default\openharmony\ets\api\application\ApplicationContext.d.ts:540
```

即：**资源目录分工不变**（`base/`=浅色值、`dark/`=深色值，令牌名相同），
开关只负责在启动时（`EntryAbility.onCreate`，内容加载前）与用户切换时调用
`setColorMode(DARK|LIGHT)`，`$r('app.color.*')` 便会按被覆盖的 colorMode 解析到对应目录 ✓。
好处：**不需要**把颜色改成 ArkTS 常量表、也不需要给每个组件传主题参数 ✗。
代价：开机瞬间若系统色与偏好不一致，理论上有一帧闪烁 —— 故必须在 `onCreate` 里设，且启动窗口背景色要按偏好设。


## 四、Token 化进度（每步单独提交、可回滚）

| 步 | 内容 | 状态 |
|:--:|:--|:--|
| 1 | 新建 `color.json`（base+dark 各一套，名称=令牌名）+ `float.json` 增字号/圆角/间距 | ✅ 完成（base 与 dark 当时同值，故零观感变化） |
| 2a | 不透明色值 + 字号**机械替换**为 `$r(...)` | ✅ 完成：684 处（`tokenize_colors.js`，24 文件） |
| 2b | 位置判定升级（正则 → 证法 A「最内层调用是 ResourceColor 属性」/ 证法 B「三元兄弟分支已是令牌」） | ✅ 完成：+56 处（`tokenize_colors.js` v2） |
| 2c | **半透明 rgba → 13 个语义令牌**（金/褐金/墨/朱/青 × 弱中强，墨为 scrim） | ✅ 完成：100 处（`overlay_tokens.js --plan/--apply`） |
| 3a | 帮助函数返回类型 `string` → `ResourceColor`（33 处仍卡在"类型为 string"） | ⬜ 待做（浅色主题正确性依赖它） |
| 3b | `PanDisk.ets` 画布色值运行时取令牌（`resourceManager.getColorSync`） | ⬜ 待做 |
| 4 | `base/element/color.json` 写入**浅色值**（A 偏金黄） | ⬜ 待做 |
| 5 | 开关落地：`setColorMode` + 偏好持久化 + 启动窗口背景色 | ⬜ 待做 |
| 6 | 新增"未登记字面色值"门禁 + `contrast_audit` 扩成**浅/深双跑** | ⬜ 待做 |

> 回滚点：tag `pre-visual-token-20260916`（Token 化之前）。
> 残留白名单（51 处）分类见《剩余色值分类清单.md》，每处都写明"为何不能令牌化"。

