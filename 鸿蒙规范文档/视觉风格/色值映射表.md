# 色值 / 字号 → 设计令牌 映射表（2026-09-16 自动生成）

> 生成方式：扫 `APP/LiurenFocusDiviner/entry/src/main/ets/**/*.ets`，把字面色值与 `dark/element/color.json`
> 的令牌值**精确比对**（rgba 先换算成 `#AARRGGBB`）。**只有精确相等者才列为"可替换"**；
> 对不上的一律列入第二节等人确认（不猜、不合并近似值 ✗）。本表生成时**未改动任何组件代码**。

## 一、可机械替换（值精确等于某令牌，替换不改变观感）

| 字面值 | 令牌 | 出现次数 | 涉及文件 |
|:--|:--|:--:|:--:|
| `#A8986E` | `ink_text_secondary` | 133 | 21 |
| `#F0D98C` | `brand_gold_bright` | 82 | 20 |
| `#C4B183` | `ink_text_muted` | 50 | 18 |
| `#F0E6C8` | `ink_text` | 44 | 9 |
| `#D8C9A3` | `ink_text_body` | 32 | 3 |
| `#1A1410` | `ink_page` | 30 | 13 |
| `#C4A25C` | `brand_gold_deep` | 28 | 12 |
| `#E9C878` | `brand_gold` | 25 | 13 |
| `#211E18` | `ink_surface` | 25 | 13 |
| `#1C1A16` | `ink_card` | 22 | 8 |
| `#B8A97F` | `ink_text_source` | 14 | 9 |
| `#F5EFE2` | `ink_text_bright` | 11 | 2 |
| `#14120F` | `pan_core` | 10 | 9 |
| `#A63A2B` | `state_warn` | 9 | 2 |
| `#E8A483` | `state_warn_text` | 9 | 7 |
| `#2B241B` | `ink_sunken` | 6 | 1 |
| `#6B5F45` | `ink_hairline` | 6 | 1 |
| `rgba(233,200,120,0.10)` | `pan_sel_bg` | 6 | 5 |
| `#D0704A` | `brand_cinnabar` | 5 | 3 |
| `#7FA0B8` | `brand_porcelain` | 2 | 2 |
| `#5FA394` | `brand_verdigris` | 1 | 1 |

**小计：21 种字面值 / 550 处 → 可替换**

## 二、未被令牌覆盖（需人工判断：归并到某令牌 or 新增令牌）

| 字面值 | 出现次数 | 涉及文件 | 建议 |
|:--|:--:|:--:|:--|
| `rgba(233,200,120,0.14)` | 17 | 6 | 中频 → 看语义是否与某令牌同义，能并则并 |
| `rgba(233,200,120,0.18)` | 11 | 10 | 中频 → 看语义是否与某令牌同义，能并则并 |
| `rgba(138,123,92,0.12)` | 10 | 2 | 中频 → 看语义是否与某令牌同义，能并则并 |
| `rgba(233,200,120,0.15)` | 10 | 4 | 中频 → 看语义是否与某令牌同义，能并则并 |
| `#7FA69A` | 8 | 6 | 中频 → 看语义是否与某令牌同义，能并则并 |
| `#2F4A5F` | 8 | 1 | 中频 → 看语义是否与某令牌同义，能并则并 |
| `rgba(233,200,120,0.12)` | 7 | 4 | 中频 → 看语义是否与某令牌同义，能并则并 |
| `rgba(233,200,120,0.2)` | 7 | 6 | 中频 → 看语义是否与某令牌同义，能并则并 |
| `#E8C46A` | 6 | 4 | 中频 → 看语义是否与某令牌同义，能并则并 |
| `#D9A94E` | 5 | 4 | 中频 → 看语义是否与某令牌同义，能并则并 |
| `rgba(240,217,140,0.16)` | 5 | 5 | 中频 → 看语义是否与某令牌同义，能并则并 |
| `#3E5C76` | 4 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(33,30,24,0.8)` | 4 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.30)` | 4 | 4 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#8FA88C` | 4 | 2 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#F5D98E` | 4 | 3 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#7A2E2E` | 3 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.45)` | 3 | 3 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.16)` | 3 | 2 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#8FA3A8` | 3 | 2 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.35)` | 3 | 3 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(107,95,69,0.2)` | 3 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0)` | 3 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(166,58,43,0.25)` | 2 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(47,74,95,0.20)` | 2 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(33,30,24,0.85)` | 2 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.22)` | 2 | 2 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#9FB6A8` | 2 | 2 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#D98C5F` | 2 | 2 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.55)` | 2 | 2 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#FDF3D0` | 2 | 2 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.40)` | 2 | 2 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.07)` | 2 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#17150F` | 2 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#4A3A18` | 2 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.25)` | 2 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.08)` | 2 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(240,217,140,0.12)` | 2 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#33705E` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#8A5A2B` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#5A4F3D` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#4A3F32` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(166,58,43,0.08)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#00000000` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(166,58,43,0.15)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#E08A7A` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(166,58,43,0.10)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#EEF3F6` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(62,92,118,0.25)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(47,74,95,0.10)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(122,46,46,0.10)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#A8843C` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#8A9BA8` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(138,155,168,0.2)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(122,110,85,0.08)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(122,110,85,0.4)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(168,152,110,0.08)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(168,152,110,0.5)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#D8CBA8` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#9A8C6E` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(240,217,140,0.85)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(138,123,92,0.14)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#C0704A` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(120,110,90,0.25)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.60)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(240,217,140,0.25)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(208,170,74,0.10)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(120,150,120,0.08)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(232,196,106,0.35)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(143,168,140,0.22)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(208,112,74,0.08)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(208,112,74,0.28)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#141210` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(208,112,74,0.14)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(138,123,92,0.10)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(208,112,74,0.45)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(138,123,92,0.30)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(208,112,74,0.16)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(208,112,74,0.55)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(240,217,140,0.14)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(26,20,16,0.72)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(26,20,16,0.85)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(208,112,74,0.10)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(208,112,74,0.35)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(107,95,69,0.12)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(232,196,106,0.10)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(232,196,106,0.30)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#16130F` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#0B0A08` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `#1E1508` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.26)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.05)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(233,200,120,0.06)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |
| `rgba(196,162,92,0.85)` | 1 | 1 | 低频（≤4 处）→ 可保留字面值或并到最近令牌 |

**小计：94 种 / 221 处待定**

## 三、字号 → 排印令牌

| 现字号 | 令牌 | 出现次数 |
|:--:|:--|:--:|
| 24 vp/fp | `font_display_s` | 9 |
| 20 vp/fp | `font_title_m` | 2 |
| 17 vp/fp | `font_title_s` | 5 |
| 16 vp/fp | `font_subtitle_l` | 33 |
| 15 vp/fp | `splash_sub` | 8 |
| 14 vp/fp | `font_subtitle_m` | 60 |
| 13 vp/fp | `font_subtitle_s` | 22 |
| 12 vp/fp | `font_body_m` | 58 |
| 11 vp/fp | `font_body_s` | 88 |
| 10 vp/fp | `font_caption_l` | 67 |
| 9 vp/fp | `font_caption_m` | 33 |

**非精确对应的字号（替换会改变字号 ✗，故不自动替换）**：48（1 处）、40（1 处）、30（1 处）、26（2 处）、22（2 处）、18（4 处）、8（9 处）、7（1 处）

## 四、下一步

1. 你确认第二节的处置（归并/新增）后，我生成最终映射并**机械替换**（分批提交）；
2. 替换后：HAP 构建 + 与现状截图比对 + 组件/排版/空态/合规门禁 + 免费版同步校验；
3. 之后才做「未登记字面色值」判据与 `contrast_audit` 双主题扩跑。

