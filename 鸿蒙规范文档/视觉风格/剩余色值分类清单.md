# 剩余色值分类清单（2026-09-16，自动生成；只统计不改码）

> 已令牌化 840 处（第 2 步 684 + 第 3 批 56 + 第 4 批 100）；本表统计**剩余**字面色值的位置类型，用于确定"哪些还能安全替换、哪些必须保留"。
> 分类依据：白名单文件 / 渐变数组 / 富文本或模板串 / 字符串拼接 / 无对应令牌的 rgba / 无对应令牌的 hex / 安全但漏网。

| 类别 | 处数 | 涉及文件 | 处置建议 |
|:--|:--:|:--:|:--|
| 白名单文件 | 163 | components/AncientCaseGallery.ets(88)、pages/Legal/PrivacyPolicy.ets(44)、pages/Legal/UserAgreement.ets(27) | **保留**（附原因登记白名单）—— 富文本拼接会让语法崩、系统栏/string 属性不接受 Resource |
| 渐变/颜色数组 | 17 | pages/Splash.ets(11)、pages/Index.ets(4)、pages/Home.ets(2) | **保留**（`linearGradient` 的颜色数组不接受 Resource） |
| 富文本或模板串 | 0 | — | **保留**（色值嵌在长文本里） |
| 字符串拼接 | 0 | — | **保留**（拼接表达式不是资源引用位置） |
| rgba 无对应令牌 | 148 | pages/Index.ets(52)、components/AncientStudy.ets(19)、components/PanDisk.ets(11) | ✅ **已收**：→ 13 个半透明语义令牌，100 处（见下节与《设计令牌清单》§三之二） |
| hex 无对应令牌 | 37 | pages/Index.ets(8)、components/AncientStudy.ets(7)、components/RuleHealthPanel.ets(7) | 🟡 **部分已收**（本轮又收 56 处），余下低频值待定：新增令牌 vs 并入最近令牌 |
| 安全但漏网 | 81 | pages/Index.ets(23)、components/PanDisk.ets(14)、components/YongShenPills.ets(6) | ✅ **已消除**：其实不是"漏网"，是旧正则太窄；位置判定升级后逐处归因完毕（见下节） |

> ⚠ 上表是**第 2 步开工前**的快照，保留用于对照。**唯一真源是工具输出**：
> `node _tools/tokenize_colors.js --dry --explain` 与 `node _tools/overlay_tokens.js --plan`；
> 本清单只是它们的摘要。

## 🆕 收尾后的准确残留（2026-09-16 第 3、4 批之后）

旧表把 81 处记为"安全但漏网"，实测原因是**旧正则太窄**：漏掉的是三元分支、多行 builder 属性
（`.border({` 换行后 `color: 'rgba(…)'`）这类位置。位置判定换成
「证法 A：最内层调用括号的函数名是 ResourceColor 属性 / 证法 B：紧邻的三元兄弟分支已是
`$r('app.color.…')`」之后，这 81 处**逐处有了结论**，不再有"不确定能不能换"的灰色地带。

当前**不能令牌化**的残留（原因明确，`--plan` 可复现）：

| 原因 | 处数 | 说明 |
|:--|--:|:--|
| 帮助函数/局部量**声明为 `string`** | 33 | 如 `TagBadge.ets` 的 `private bgColor(): string`：**调用处是颜色属性、函数返回类型却是 string**。需把返回类型改成 `ResourceColor`（第 3a 步）——**浅色主题的正确性依赖它** |
| 排除文件 | 15 | `AncientCaseGallery.ets`（属性声明为 string）、Legal 两页（富文本拼接会让语法崩） |
| 渐变/模板串 | 2 | `linearGradient` 的颜色数组与模板串不接受 Resource |
| 离三档过远的实心填充 | 1 | `DuyuCard` 金 0.85 实心填充（Δα>0.12；两主题下同为金色实心，无需令牌） |

**工具化纪律**（踩过的坑，勿重犯）：`color.json` 里已写入的令牌会让下一轮 `--plan`
把同值站点误当成"既有令牌"而漏掉 —— **重新规划前必须先 `git checkout` 到干净状态**再跑。
另：修订位置判定时曾用"只看三元形状"的宽松规则（`/\?[^?:]*:\s*$/`），
把 `isKong ? 'rgba(a)' : 'rgba(b)'` 这种**纯字面值三元**误判为安全 → 构建编译错
（canvas 的 `ctx.fillStyle` 不接受 Resource）。教训：**"附近有令牌"不等于"兄弟分支是令牌"**。

**旧快照合计 446 处**（第 2 步开工前的口径；现已按上节逐处归因完毕，勿再引用此数）

## 无对应令牌的 hex（按出现次数，前 20）

| 色值 | 处数 |
|:--|:--:|
| `#E8C46A` | 6 |
| `#7FA69A` | 5 |
| `#8FA88C` | 4 |
| `#8FA3A8` | 3 |
| `#9FB6A8` | 2 |
| `#D98C5F` | 2 |
| `#FDF3D0` | 2 |
| `#4A3A18` | 2 |
| `#A8843C` | 1 |
| `#8A9BA8` | 1 |
| `#D8CBA8` | 1 |
| `#D9A94E` | 1 |
| `#9A8C6E` | 1 |
| `#C0704A` | 1 |
| `#141210` | 1 |

## 无对应令牌的 rgba（按出现次数，前 12）

| 字面值 | 处数 | 换算 |
|:--|:--:|:--|
| `rgba(233,200,120,0.14)` | 16 | `#24E9C878` |
| `rgba(233,200,120,0.18)` | 11 | `#2EE9C878` |
| `rgba(138,123,92,0.12)` | 10 | `#1F8A7B5C` |
| `rgba(233,200,120,0.15)` | 10 | `#26E9C878` |
| `rgba(233,200,120,0.12)` | 5 | `#1FE9C878` |
| `rgba(240,217,140,0.16)` | 5 | `#29F0D98C` |
| `rgba(233,200,120,0.20)` | 5 | `#33E9C878` |
| `rgba(33,30,24,0.8)` | 4 | `#CC211E18` |
| `rgba(233,200,120,0.30)` | 4 | `#4DE9C878` |
| `rgba(233,200,120,0.45)` | 3 | `#73E9C878` |
| `rgba(233,200,120,0.16)` | 3 | `#29E9C878` |
| `rgba(107,95,69,0.2)` | 3 | `#336B5F45` |
