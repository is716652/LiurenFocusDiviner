# AGENTS.md —— 自动加载用的指路牌（零事实）

**本仓库的唯一权威文档是 [`Agent.md`](Agent.md)。** 本文件不记录任何事实、不重复任何口径，
只做一件事：让**自动加载工作区指令文件**的 AI 会话知道该去读哪一份 ——
DSH 默认只自动加载 `AGENTS.md` / `CLAUDE.md`，不会自动加载 `Agent.md`。

**如果你是刚接手的 AI 会话**：先读 [`Agent.md`](Agent.md) **§0.1「接手检查清单」** ——
那里写明第一件事做什么（读哪几节、跑什么命令确认这棵树是好的、状态去哪看），
以及在 §11「当前状态与下一步」里哪些待办**只能由用户做**、哪些可以自己推进。

三方分工（避免多处描述各自漂移）：

| 文件 | 角色 |
|:--|:--|
| [`Agent.md`](Agent.md) | **唯一权威**：真源边界、门禁体系、免费版同步、构建出包、版本递进、配色与对比度纪律、发布记录、待办 |
| [`README.md`](README.md) | 给人看的门面 + 索引（同样零事实） |
| `AGENTS.md`（本文件） | 自动加载用的指路牌（零事实） |

需要改口径时改 `Agent.md`，本文件与 README 都无需跟着改。
