# AGENTS.md —— 自动加载用的指路牌（零事实）

**本仓库的唯一权威文档是 [`Agent.md`](Agent.md)。**

本文件不记录任何事实、不重复任何口径，只做一件事：让**自动加载工作区指令文件**的 AI 会话知道该去读哪一份 ——
DSH 默认只自动加载 `AGENTS.md` / `CLAUDE.md`，**不会**自动加载 `Agent.md`。
正因如此，本文件必须存在且必须保持极短：它的全部价值就是保住这条入口。

**如果你是刚接手的 AI 会话**：

1. 先读 [`Agent.md`](Agent.md) **§0.1「接手检查清单」** —— 第一件事做什么、跑什么命令确认这棵树是好的；
2. 再看 **§11「当前状态与下一步」** —— 哪些待办**只能由用户做**、哪些可以自己推进；
3. 版本状态与包指纹在 **§8**；文件分工（本文件 / `README.md` / `Agent.md` 各管什么）在 **§13.1**。

**改口径时只改 `Agent.md`** —— 本文件与 `README.md` 都无需跟着改。
