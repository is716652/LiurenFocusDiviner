feat(engine): 阶段4 点宫速查只读接口 palaceLookup（只读，不做 UI）

为 Agent.md §14.4「点宫速查卡」提供引擎侧只读接口，UI 接线留待第 2 优先级。
两侧同源：core/liuren/pan/dx.ts 与 model/pan/dx.ets（门面转发 LiurenCore.palaceLookup）。

接口：LiurenCore.palaceLookup(chart, 地盘宫, 用神支) -> PalaceLookup
一屏给全该支：gong / tianZhi / wuXing / yinYang / liuQin（与日干六亲）/
relToRiGan（生干/克干/干生/干克/比和）/ relToYongShen（生用神/克用神/用神生/用神克/比和/
未选用神）/ qiJi（十二宫气机点）/ kong / shensha[] / jiang（所乘天将）/
dun·dunRi·dunShi（旬遁 + 日干遁 + 时干遁）/ role（该支在本课的角色：
用神·初/中/末传·日干寄宫·临日支·月将宫·贵人宫，另给一句话 role.text）。

入参契约（本阶段澄清，重要）：地盘宫与天盘支取值域**都是十二支、数学上不可分**，
故契约明确为「**入参是地盘宫**」（UI 点盘点到的就是宫）；传非宫但合法的支时按天盘支
尽力反查；非法值按空宫处理。行为因此确定、可断言（原先「传天盘支先反查」的措辞
会诱导出不可判的调用，已订正）。

新增门禁 _tests/_test_palace.js（P1–P7；全套 27 个测试全绿）：
- P1 3 盘 × 12 宫字段齐全、取值域合法（气机点长生/墓各唯一）
- P2 **纯只读**：12 宫 + 边界调用前后盘对象逐字节相同
- P3 逐项与盘面/盘态/规则表对账（天将·旬遁干·日干遁干·时干遁干·空亡·气机点·神煞·五行）
- P4 六类角色标记全部正确（传位/用神/日干寄宫/临日支/月将宫/贵人宫）
- P5 入参分辨确定性（合法支恒按地盘宫解析；反查与 tp 映射逐支一致；抽样无第三宫落点）
- P6 边界：空串/非法支/用神留空均不崩；用神留空显式写「未选用神」（不留白）
- P7 接口在 core 侧与 ArkTS 侧同名同签名（源码级核对）

同时为「缺表自述」补测：_tests/_test_empty_state.js（上一提交）已覆盖 E1–E6。

回归（全过）：行为快照 185981 条逐条一致（总哈希未变）；对外 API 0 缺失、31 常量值一致；
27 个测试全绿；主版与免费版 HAP 均 BUILD SUCCESSFUL；语料对账全部一致；配色 0 违规；
免费版 verify PASS；build_core.js --check exit 0。

遗留待办（已写进 Agent.md §14）：
1. UI 侧点宫弹卡尚未接线（第 2 优先级，与组件化同期做）
2. 三张「App 加载但引擎未读」的规则表（十二宫气机点/空亡规则/助日规则）应在卡内
   作为可点开的规则出处呈现，不得继续「加载了但没人读」地悬着
3. ArkTS 侧 ruleHealth/missingRules 仍未提供（arkts-no-props-by-index），
   改由 DataLoader 逐表状态承担；UI 徽标与缺表清单要吃这两侧状态
