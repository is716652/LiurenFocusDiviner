feat(core): 速查卡行装配收回引擎（readXiangCard）+ R7/R8 自检断言；ArkTS 镜像本轮未通过，已如实回退

目标（方案 B 第一步）：把点宫速查卡的装配逻辑从 App 侧手写文件收回真源 core，避免两套实现漂移。

引擎侧（已完成、已验证）：
- core/liuren/pan/dx.ts 新增 `readXiangCard(c, 地盘宫, 用神支): Record<string, string>[]`
  —— 行序照《以炁为基点读象》：此宫 → 远景（旺衰）→ 中景（冲·合·害·刑·空）→ 近景（与日干/用神 · 乘将 · 遁干）
  → 神煞 → 气机点（宫位 × 表内象义 · 冲宫 · 合宫 · 三合 × 空亡三态）→ 原文（单独成行）→ 此宫一句。
  数据全部来自同文件的 palaceLookup / qijiReading / zhuriWhy 与引擎已算的 c.dx.relations，不新增表读取。
- cardRow() 由 private 提升为 public（抽取脚本按 `static NAME(` 匹配，需镜像到 .ets；与 §13 那批可见性放宽同性质）。
- core 门面转发（_core_assemble.js 独立守卫，勿挂进那块「facade 里没有 ruleHealth」的老守卫，否则被静默跳过）。

自检断言（新增，全绿）：
- _tests/_test_readxiang.js 追加 R7/R8：行序契约（此宫/远景/中景/近景/气机点/此宫一句 的位置关系）、
  气机点行含宫位名与空亡状态说明、原文单独成行且有内容、末行与 qijiReading().text 逐字一致（同口径）、
  各行 text 不含现实结论词、每行带 tone、缺表时明说未加载、改表即改输出（真读表）、非法入参只回一行原因。
- R7 首次运行即抓出一个真缺陷并已修：缺表时宫位名仍算得出，原判据只看 gong 非空 → 该行既无象义也无说明（静默）；
  现改为把 qijiReading().note 写进行内并把 tone 降为警示。

ArkTS 镜像（本轮未完成，已按纪律回退到编译通过状态）：
- 做了：_ets_facade_extras 抽取四方法、_ets_split 的 FORWARD 转发、types.ets 注三键声明、
  DataLoader.Ext 声明改 Record<string, Object>；主版构建仍在 `rules.duxiang[...]` 的读取上报
  arkts-no-props-by-index（即便声明为 Record<string, Object> 也拒绝），门面还一度出现重复实现。
- 处置：撤回上述四处生成链改动 + 还原 ArkTS 生成物（git checkout + sync），使两侧 HAP 恢复 BUILD SUCCESSFUL；
  App 侧仍走原有 ReadXiang.ets（产品路径不受影响）。
- 下一步方向（已缩小范围）：ArkTS 侧改从「引擎已算好的结构」出数（如 Duxiang.relations 那样的 Record 字段访问），
  或为每张表提供一个 .ets 侧已能编译的读取入口，避免在 .ets 里直接下标 rules.duxiang。

回归：32/32 测试全绿；行为快照 185981 条逐条一致（定法零改动）；配色 0 违规；门禁自检 11/11；
主版与免费版 HAP 均 BUILD SUCCESSFUL。
