test(gate): 阶段3 门禁逐模块生效 + 无声空态断言 + 重建链收敛为单入口

对应 Agent.md §14.7 第 2 条「无声空态 = 测试失败」，并收掉组件化留下的工具链隐患。

新增门禁 _tests/_test_empty_state.js（E1–E6；全套 26 个测试全绿）：
- E1 缺表必被点名：按 DataLoader 真实口径逐个场景（键缺失 → 字段 undefined）注入，
  missingRules() 必须点出受影响规则表（旺衰/基础关系/十二宫气机点/空亡规则/助日规则/
  神煞/毕法/行年 共 8 张）
- E2 内层键缺失也要看得见（旺衰休囚死.旺衰、行年打分表缺 kong/bands）
- E3 「表在但无条目」≠「缺表」：loaded=true/entries=0 与 loaded=false 不同形
- E4 缺表照旧出盘（不得整体 fail-fast），但受影响栏位必须可见地变化
- E5 缺表 note 不得出现数据文件名（.json），且 loaded=false 项 note 必须非空
- E6 行年不得静默消失：整表缺失走内置默认表；键缺失必须被点名（不得静默算成 NaN）

门禁口径：
- A2 收紧：例外只开给 .ets（ArkTS 唯一模块机制）；.ts 侧任何 import/export 都违规
  （实测 core/liuren/** 零 import，原「import type 例外」是零使用的白口子，已收回）。
  收紧后做变异测试：注入值 import / import type / export 三者均被判否，逐字节还原后复跑通过。
- A3 订正（上一提交已含）：块注释区间改为字符级判定；非注释代码成组出现天将名仍违规。
- 两个门禁的扫描面按目录覆盖全部模块（core/liuren/** 与 model/pan/*.ets 自动在册）。

工具链收敛（任一步漏跑会产出「能编译但语义不全」的半成品，故必须单入口）：
- 新增 _tools/rebuild_core.js：core 侧唯一入口（切片 → 补接口 → 门面/可见性 → 装配编译）
- _tools/_ets_pipeline.js：ArkTS 侧唯一入口（已存在）
- 工具正式改名（原临时名易被当草稿误删，且文件头旧名已找不到）：
  _core_split.js / _core_api_extras.js / _core_assemble.js / build_core.js
  _ets_facade_extras.js / _ets_split.js / _ets_qualify.js
- _core_split.js 基线改为从 tag v1.0.4-pre-componentize 取，并加行数守卫
  （磁盘上的 core/liuren-core.ts 已是装配产物，不能再当切片基线）
- 删除自带「用完即删」字样的 _tools/_run_all_tests.js

Agent.md：§9/§10/§13 补写组件化后的真源纪律、唯一重建入口、5 个可见性放宽成员；
§14.2 补「缺表提示不得出现数据文件名」；§14.3 补缺失清单 API 现状与 UI 侧待办；
§13 的 A2 段按收紧后的口径修订。

回归：26 个测试全绿；行为快照 185981 条逐条一致；对外 API 0 缺失、31 个常量值一致。
